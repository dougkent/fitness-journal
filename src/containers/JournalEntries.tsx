// React
import React from 'react';

// Material UI
import { Typography } from '@material-ui/core';

const JournalEntries: React.FC = () => {
    return <Typography variant='h3'>Journal Entries</Typography>;
};

export default JournalEntries;
